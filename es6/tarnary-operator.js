// ternary operator

const number = 10
// if condition true = 'even' or 'odd'
const result = number % 2 === 1 ? 'Even' : 'ODD'
console.log(result)


// short-end

const name = 'riad'
// if name has empty/'' than = 'MH.riad'
const result2 = name || 'MH.Riad'
console.log(result2)

// if name has value than = print
const result3 = name && console.log('Name has value')

