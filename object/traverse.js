import { studentsArrayData, StudentsObjectData } from "./data.js";

/**
 * array traverse --> filter, map, forEach, reducer, find, some
 */

/**
 * traverse in object
 */
Object.values(StudentsObjectData).forEach(item => {
    console.log(item)
})